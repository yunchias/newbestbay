<%= form_for(@bidcart) do |f| %>
  <% if @bidcart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bidcart.errors.count, "error") %> prohibited this bidcart from being saved:</h2>

      <ul>
      <% @bidcart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
